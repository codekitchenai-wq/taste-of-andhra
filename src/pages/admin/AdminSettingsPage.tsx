import { PagePlaceholder } from '@/components/common/PagePlaceholder'

export default function AdminSettingsPage() {
  return (
    <PagePlaceholder
      title="Settings"
      description="Restaurant configuration and admin preferences."
    />
  )
}
